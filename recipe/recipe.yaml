schema_version: 1

context:
  name: sqlalchemy-hana
  version: "4.2.0"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/${{ name | replace('-', '_') }}-${{ version }}.tar.gz
  sha256: 54c089821f8d62da3e340972946fbab2eeea230a217feefd53919bf06b238677

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
  run:
    - python >=${{ python_min }}
    - sqlalchemy >=1.0.0

tests:
  - python:
      imports:
        - sqlalchemy_hana
      python_version: ${{ python_min }}.*
  - requirements:
      run:
        - pip
        - python ${{ python_min }}.*
    script:
      - pip check

about:
  summary: SQLAlchemy dialect for SAP HANA
  license: Apache-2.0
  license_file: LICENSE
  homepage: https://github.com/SAP/sqlalchemy-hana/

extra:
  recipe-maintainers:
    - swenger
    - janjagusch
