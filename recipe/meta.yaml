{% set name = "sqlalchemy-hana" %}
{% set version = "1.0.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/sqlalchemy-hana-{{ version }}.tar.gz
  sha256: 3ca1aecc16757cfebf29dc0369229afab3d8a50e796995fe9adefd79ffbb155e

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python >=3.6
    - pip
  run:
    - python >=3.6
    - sqlalchemy >=1.0.0

test:
  imports:
    - sqlalchemy_hana
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/SAP/sqlalchemy-hana/
  summary: SQLAlchemy dialect for SAP HANA
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - swenger
    - janjagusch
